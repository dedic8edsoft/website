export const config = {
  siteName: 'Bingo',
  title: 'Bingo',
  description: '',
  locale: 'en',
  baseUrl: process.env.baseUrl,

  siteDomain: process.env.siteDomain,
  apiServer: process.env.apiServer,
};
